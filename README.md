# The Astrological Estimator #
## Price-Volume Strength ##

We define ${S(t)}$ the **price-volume strength** for the asset price $p(t)$ as:

${S(t) = \frac {2(p_t-p_{t-1})}{p_t+p_{t-1}} e^{(v_t-v_{t-1})}}$,

## Heliocentric Longitude Aggregator ##

Let ${λ_i(t)}$ be the heliocentric longitude of planet ${i}$ at time ${t}$. We define the **heliocentric longitude aggregator** ${Λ(t)}$ as:

${Λ(t)=g \sum_{j=1,i=1}^{J,J}(λ_i(t)-λ_j(t))}$.

${g=\sum_{i=1}^N \frac{m_i}{d_i^2}}$,

${m_i}$ is the mass of planet ${i}$ relative to earth's,

${d_i}$ is the distance between planet ${i}$ and earth and


